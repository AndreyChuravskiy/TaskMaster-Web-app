To create container from docker-compose.yml:
    1)Place docker-compose.yml and DockerFile(from this folder) to TaskMaster-Web-app
    2)Use: mvn clean package -DskipTests
    3)Use: docker-compose up